services:
  portfolio_app:
    container_name: portfolio_app
    labels:
      - traefik.enable=true
      - traefik.http.routers.portfolio_app.rule=Host(`${DOMAIN}`)
      - traefik.http.services.portfolio_app.loadbalancer.server.port=80
